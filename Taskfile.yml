version: "3"

tasks:
  docs:
    cmds:
      - swag i -d ./cmd/http,./internal/collection,./internal/gallery,./internal/domain --parseDependency --parseInternal
  grun:
    cmds:
      - go run ./cmd/http/main.go
  rrun:
    dir: "web"
    cmds:
      - npm run dev
  ginstall:
    cmds:
      - go install github.com/swaggo/swag/cmd/swag@latest
# TODO add dockerfiles, docker compose and add a task cmd to handle it all
